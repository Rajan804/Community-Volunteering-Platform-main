// blurAnimation.js
document.addEventListener('DOMContentLoaded', function () {
    // Add the blur-animation class to the body after a slight delay
    setTimeout(function () {
        document.body.classList.add('blur-animation');
    }, 500); // delay in milliseconds
});
